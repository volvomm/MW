public static class StoryProgress
{
    public static bool HasTalkedToDevilDog = false;

    public static bool MotherCatRescueDialogueFinished = false;

    public static bool RecorderPuzzleFinished = false;

    public static bool DevilDogLureDialogueFinished = false;

    public static bool DevilDogTrapdoorSequenceFinished = false;

    public static bool ClosetBarricaded = false;

    public static bool MotherCatReunited = false;


    // =========================================================
    // KITTEN STORY PROGRESSION
    // =========================================================

    // Becomes true after Patch finishes the initial group
    // conversation with the kittens.
    public static bool KittenGroupDialogueFinished = false;

    // Becomes true after Mother Cat tells Patch to talk
    // to the kittens for the puzzle-box number hints.
    public static bool KittenNumberHintsUnlocked = false;
}